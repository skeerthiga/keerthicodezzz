#include <stdio.h>
int main()
{
int l, n,y1= 0,y2=1,nextTerm;
printf("Enter the number of terms: ");
scanf("%d", &n);
printf("Fibonacci Series: ");
for (l= 1;l<= n;++l)
{
printf("%d",y11);
nextTerm = y1+y2;
y1=y2;
y2= nextTerm;
}
return 0;
}
