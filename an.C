#include <stdio.h> 
#include <ctype.h> 
int main() {  
char ch;
printf("Enter a Ch\n");  
scanf("%c", &ch);  
if(isalpha(character)) {  
printf("%c is an Alphabet\n", ch);  
} 
else 
{  
printf("%c is Not an Alphabet\n", ch);  
}
return 0;  
}
