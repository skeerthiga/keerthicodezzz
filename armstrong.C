
#include<stdio.h>
int main()
{
int num,l,sum=0,t;
printf("Enter a number: ");
scanf("%d",&num);
t=num;
while(num!=0)
{
r=num%10;
num=num/10;
sum=sum+(l*l*l);
}
if(sum==temp)
printf("%d is an Armstrong number",t);
else
printf("%d is not an Armstrong number",t);
return 0;
}
